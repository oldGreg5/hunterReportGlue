services:
  app:
    build: .
    ports:
      - "8089:8081"
    volumes:
      - ./data/HunterWyniki.txt:/usr/src/app/static/HunterWyniki.txt
    restart: always
    environment:
      - RUST_LOG=info
